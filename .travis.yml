language: node_js
node_js:
  - 'stable'
  - '12'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-chrome-extension-kickstart' ]; then cd .. && eval "mv $currentfolder generator-chrome-extension-kickstart" && cd generator-chrome-extension-kickstart; fi
